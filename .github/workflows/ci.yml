name: CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
        with:
          # Pinned to the dev machine's version: 'latest' broke the fetch-stubbing
          # tests (different global fetch behavior). Bump deliberately.
          bun-version: "1.3.13"
      - name: Install
        run: bun install --frozen-lockfile
      - name: Test
        run: bun test
      - name: Typecheck
        run: npx tsc --noEmit
      - name: Lint
        run: bun run lint
      - name: Build
        run: bun run build
